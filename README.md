# Simple Port Scanner

A lightweight educational tool to scan open TCP ports on a target host using Python's built-in `socket` library.

## 🔍 Purpose
- Learn how network scanning works
- Understand basic concepts of information security
- Practice Python scripting for OSINT and reconnaissance

## ✅ Features
- Scans a user-defined range of ports
- Real-time console output with clear status (open/closed)
- Saves results to `scan_result.txt`
- No external dependencies — uses only standard Python libraries

## ▶️ How to Run
1. Clone or download the script
2. Open terminal and run:
   ```bash
   python port_scanner.py
